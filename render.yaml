services:
    - type: web
    name: vektorflow-backend-router
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
            - key: META_APP_ID
        sync: false
            - key: META_APP_SECRET
        sync: false
